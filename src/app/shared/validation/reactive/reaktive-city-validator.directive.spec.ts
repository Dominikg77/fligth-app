import { ReaktiveCityValidatorDirective } from './reaktive-city-validator.directive';

describe('ReaktiveCityValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ReaktiveCityValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
